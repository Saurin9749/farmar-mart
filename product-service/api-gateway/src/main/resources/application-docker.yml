server:
  port: 8080


eureka:
  client:
    service-url:
      defaultZone: http://api-gateway:8761/eureka/

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: order_route
          uri: http://localhost:8091
          predicates:
            - Path=/orders/**
        - id: inventory_route
          uri: http://localhost:8092
          predicates:
            - Path=/inventory/**
